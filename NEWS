NEWS
****

New in 1.1.1:

  * Let git-pull-all continue if an error occurrs while pulling a repository.

New in 1.1.0:

  * Add -a, --all to print outgoing changes on any branch.
  * Use current directory if no arguments are given.

New in 1.0.0:

  * Check whether a repository has changesets to push.
  * List the changesets to push.
  * Detect whether a branch has no remote configured.
  * Detect whether HEAD is detached
